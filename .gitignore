# Environment
.env
.env.local

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Model artifacts (large files — use S3)
models/*.bin
models/*.gguf
models/*.safetensors
models/*.pt

# Training artifacts
*.log
wandb/
runs/
checkpoints/

# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform.lock.hcl
terraform/terraform.tfvars

# OS
.DS_Store

# Claude config — tracked in git, copied into image at build time
# .devcontainer/claude-config/
